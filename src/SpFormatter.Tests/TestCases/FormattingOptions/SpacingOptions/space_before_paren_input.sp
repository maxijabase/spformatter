void test()
{
    if(flag)
    {
        PrintToChat(client, message);
    }
    switch(value)
    {
        case 1:
            that();
    }
    for(int i = 0; i < n; i++)
    {
        while(running)
        {
            that();
        }
    }
}
