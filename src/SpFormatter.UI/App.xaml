<Application x:Class="SpFormatter.UI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Cool slate workbench: ink text, paper editors, teal accent -->
            <Color x:Key="BgDeepColor">#FF141A22</Color>
            <Color x:Key="BgShellColor">#FF1B2430</Color>
            <Color x:Key="BgPanelColor">#FF243040</Color>
            <Color x:Key="BgEditorColor">#FFF7F8FA</Color>
            <Color x:Key="BgEditorDarkColor">#FF1E1E1E</Color>
            <Color x:Key="InkColor">#FF101820</Color>
            <Color x:Key="InkDarkColor">#FFD4D4D4</Color>
            <Color x:Key="MutedColor">#FF6B7785</Color>
            <Color x:Key="MutedDarkColor">#FF858585</Color>
            <Color x:Key="LineColor">#FF314056</Color>
            <Color x:Key="AccentColor">#FF0F8A7A</Color>
            <Color x:Key="AccentSoftColor">#FFD7F3EE</Color>
            <Color x:Key="OkColor">#FF1F7A4D</Color>
            <Color x:Key="WarnColor">#FF9A6700</Color>
            <Color x:Key="ErrColor">#FFB42318</Color>
            <Color x:Key="ChipBgColor">#FF2A3648</Color>

            <SolidColorBrush x:Key="BgDeepBrush" Color="{StaticResource BgDeepColor}"/>
            <SolidColorBrush x:Key="BgShellBrush" Color="{StaticResource BgShellColor}"/>
            <SolidColorBrush x:Key="BgPanelBrush" Color="{StaticResource BgPanelColor}"/>
            <SolidColorBrush x:Key="BgEditorBrush" Color="{StaticResource BgEditorColor}"/>
            <SolidColorBrush x:Key="BgEditorDarkBrush" Color="{StaticResource BgEditorDarkColor}"/>
            <SolidColorBrush x:Key="InkBrush" Color="{StaticResource InkColor}"/>
            <SolidColorBrush x:Key="InkDarkBrush" Color="{StaticResource InkDarkColor}"/>
            <SolidColorBrush x:Key="MutedBrush" Color="{StaticResource MutedColor}"/>
            <SolidColorBrush x:Key="MutedDarkBrush" Color="{StaticResource MutedDarkColor}"/>
            <SolidColorBrush x:Key="LineBrush" Color="{StaticResource LineColor}"/>
            <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
            <SolidColorBrush x:Key="AccentSoftBrush" Color="{StaticResource AccentSoftColor}"/>
            <SolidColorBrush x:Key="OkBrush" Color="{StaticResource OkColor}"/>
            <SolidColorBrush x:Key="WarnBrush" Color="{StaticResource WarnColor}"/>
            <SolidColorBrush x:Key="ErrBrush" Color="{StaticResource ErrColor}"/>
            <SolidColorBrush x:Key="ChipBgBrush" Color="{StaticResource ChipBgColor}"/>
            <SolidColorBrush x:Key="OnAccentBrush" Color="#FFFFFFFF"/>

            <FontFamily x:Key="UiFont">Segoe UI Variable, Segoe UI, sans-serif</FontFamily>
            <FontFamily x:Key="CodeFont">Cascadia Code, Consolas, Courier New</FontFamily>

            <Style TargetType="Window">
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="Background" Value="{StaticResource BgShellBrush}"/>
                <Setter Property="Foreground" Value="#FFE8EEF5"/>
            </Style>

            <Style x:Key="GhostButton" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="#FFE8EEF5"/>
                <Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="12,7"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#332A3648"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.45"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource GhostButton}">
                <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource OnAccentBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>

            <Style x:Key="ChipToggle" TargetType="ToggleButton">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="#FFC5D0DC"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="12,6"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="6"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentBrush}"/>
                                    <Setter Property="Foreground" Value="{StaticResource OnAccentBrush}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.92"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="PanelLabel" TargetType="TextBlock">
                <Setter Property="Foreground" Value="#FFB8C4D1"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Margin" Value="0,0,0,8"/>
            </Style>

            <Style x:Key="OptionCheck" TargetType="CheckBox">
                <Setter Property="Foreground" Value="#FFE0E7EF"/>
                <Setter Property="Margin" Value="0,0,0,8"/>
                <Setter Property="FontSize" Value="12"/>
            </Style>

            <Style x:Key="OptionField" TargetType="TextBox">
                <Setter Property="Background" Value="#FF1B2430"/>
                <Setter Property="Foreground" Value="#FFE8EEF5"/>
                <Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="6,4"/>
                <Setter Property="CaretBrush" Value="#FFE8EEF5"/>
            </Style>

            <Style x:Key="OptionCombo" TargetType="ComboBox">
                <Setter Property="Background" Value="#FF1B2430"/>
                <Setter Property="Foreground" Value="#FFE8EEF5"/>
                <Setter Property="BorderBrush" Value="{StaticResource LineBrush}"/>
                <Setter Property="Padding" Value="4,2"/>
            </Style>

            <Style x:Key="StatusChip" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource ChipBgBrush}"/>
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="Padding" Value="10,4"/>
                <Setter Property="Margin" Value="0,0,8,0"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
